name: Gold Alpha Quant V6.0 Monitor

on:
  schedule:
    # 这里设置每 30 分钟运行一次 (UTC时间)
    - cron: '*/30 * * * *'
  workflow_dispatch: # 这个按钮允许你手动随时点火运行

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 拉取最新代码
        uses: actions/checkout@v3

      - name: 🐍 配置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 安装量化必备插件
        run: |
          pip install -r requirements.txt

      - name: 🚀 执行黄金决策引擎
        run: python main.py
