name: Gold Alpha Quant V6.0 Monitor

on:
  push:
    branches: [ main ] # 只要你一改代码，它就立刻跑一次（这是为了激活）
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch: # 这是手动按钮的“开关”

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 拉取最新代码
        uses: actions/checkout@v3

      - name: 🐍 配置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 安装量化必备插件
        run: |
          pip install -r requirements.txt

      - name: 🚀 执行黄金决策引擎
        run: python main.py
