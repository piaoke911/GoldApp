name: Gold-Quantum-App-Factory
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 首席执行：获取源代码
        uses: actions/checkout@v4

      - name: 🧠 核心架构：Buildozer 引擎启动
        # 换用这个已经内部修复了 user 权限 Bug 的稳定版本
        uses: FarasYasin/buildozer-action@v2
        with:
          buildozer_version: master
          workdir: .

      - name: 📦 成果输出：提取 APK 安装包
        uses: actions/upload-artifact@v4
        with:
          name: Gold-Signal-App-Final
          path: bin/*.apk
