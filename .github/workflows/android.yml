name: Gold-Quantum-App-Factory
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 首席执行：拉取全球量化代码
        uses: actions/checkout@v4

      - name: 🧠 核心架构：原装 Buildozer 环境启动
        # 使用 Kivy 官方提供的最原始、最底层的镜像，没有任何权限 Bug
        uses: ArtemSBulgakov/buildozer-action@v1.6.1
        with:
          buildozer_version: master
          workdir: .

      - name: 📦 成果输出：提取 APK 安装包
        uses: actions/upload-artifact@v4
        with:
          name: Gold-Signal-Final-App
          path: bin/*.apk
