name: Global-Quant-Master-Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 1. 源码同步
        uses: actions/checkout@v4

      - name: 🏗️ 2. 环境构建 (内置 NDK 补丁版)
        # 换用这个专门修复了 8秒自爆 Bug 的稳定版本
        uses: FarasYaasin/buildozer-action@v2
        with:
          buildozer_version: master
          workdir: .

      - name: 📦 3. 成果交付
        uses: actions/upload-artifact@v4
        with:
          name: GoldApp-Final
          path: bin/*.apk
