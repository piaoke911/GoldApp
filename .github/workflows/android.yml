name: Global-Quant-Master-Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 1. 首席执行：同步源代码
        uses: actions/checkout@v4

      - name: 🏗️ 2. 环境构建：使用 Kivy 官方封装镜像
        # 这个 Action 是 Kivy 官方最稳的，它内部自带了所有权限修复
        uses: ArtemSBulgakov/buildozer-action@v1.6.1
        with:
          buildozer_version: master
          workdir: .

      - name: 📦 3. 成果提取：交付全球量化终端
        uses: actions/upload-artifact@v4
        with:
          name: GoldApp-Master-Final
          path: bin/*.apk
