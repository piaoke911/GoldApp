name: Gold-Signal-Flash-Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 1. 源码同步
        uses: actions/checkout@v4

      - name: 🏗️ 2. 环境注入 (极速版)
        # 换用这个内置了 Android SDK 协议自建的 Action
        uses: FarasYaasin/buildozer-action@v2
        with:
          buildozer_version: master
          workdir: .

      - name: 📦 3. 成果提取
        uses: actions/upload-artifact@v4
        with:
          name: GoldApp-Build
          path: bin/*.apk
          
