name: Gold-Quantum-Final-Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 首席执行：同步源码
        uses: actions/checkout@v4

      - name: 🧠 核心架构：Buildozer 原生构建
        # 使用目前最稳、修复了所有权限 Bug 的版本
        uses: FarasYaasin/buildozer-action@v2
        with:
          buildozer_version: master
          workdir: .

      - name: 📦 成果输出：提取安装包
        uses: actions/upload-artifact@v4
        with:
          name: GoldApp-Final
          path: bin/*.apk
