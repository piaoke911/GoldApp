name: Global-Quant-Master-Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 1. 源码同步
        uses: actions/checkout@v4

      - name: 🏗️ 2. 环境强制初始化 (Master Edition)
        run: |
          sudo apt update
          sudo apt install -y python3-pip python3-setuptools libffi-dev libssl-dev
          pip3 install --user --upgrade buildozer

      - name: 🧠 3. 核心编译 (跳过所有权限 Bug)
        # 直接调用系统底层，强制避开 chown 错误
        run: |
          export PATH=$PATH:~/.local/bin
          buildozer android debug
        env:
          APP_ANDROID_ACCEPT_SDK_LICENSE: "1"

      - name: 📦 4. 成果交付
        uses: actions/upload-artifact@v4
        with:
          name: GoldApp-Final
          path: bin/*.apk
