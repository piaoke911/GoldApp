name: Gold Quant Monitor V6.0

on:
  schedule:
    # 这里的 cron 代表每 30 分钟运行一次。
    # 格式为：分钟 小时 日 月 周 (UTC时间)
    - cron: '*/30 * * * *'
  workflow_dispatch: # 这个选项能让你手动点击运行

jobs:
  run-quant-engine:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 拉取最新代码
        uses: actions/checkout@v3

      - name: 🐍 配置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 安装量化必备插件
        run: |
          pip install -r requirements.txt

      - name: 🚀 执行黄金量化决策引擎
        # 这一步会运行你的主程序，触发发信逻辑
        run: python main.py
